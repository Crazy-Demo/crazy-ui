/**
 * Resolver for unplugin-vue-components.
 * Generates component name → import path mappings for on-demand import.
 *
 * Placeholder — will be populated as components grow.
 */
export function resolveComponent(_name: string): { name: string; from: string } | undefined {
  // Placeholder: return undefined for now
  return undefined;
}

/**
 * Tag Component Tokens
 *
 * These tokens inherit from the theme package and can be overridden at the component level.
 */

import { tagTokens } from '@crazy-ui/theme';

export const tagComponentTokens = {
  ...tagTokens,
} as const;

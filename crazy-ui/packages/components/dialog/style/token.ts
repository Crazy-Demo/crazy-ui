/**
 * Dialog Component Tokens
 *
 * These tokens inherit from the theme package and can be overridden at the component level.
 */

import { dialogTokens } from '@crazy-ui/theme';

export const dialogComponentTokens = {
  ...dialogTokens,
} as const;

/**
 * Popover Component Tokens
 *
 * These tokens inherit from the theme package and can be overridden at the component level.
 */

import { popoverTokens } from '@crazy-ui/theme';

export const popoverComponentTokens = {
  ...popoverTokens,
} as const;

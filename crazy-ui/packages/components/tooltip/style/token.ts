/**
 * Tooltip Component Tokens
 *
 * These tokens inherit from the theme package and can be overridden at the component level.
 */

import { tooltipTokens } from '@crazy-ui/theme';

export const tooltipComponentTokens = {
  ...tooltipTokens,
} as const;

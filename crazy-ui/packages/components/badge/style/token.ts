/**
 * Badge Component Tokens
 *
 * These tokens inherit from the theme package and can be overridden at the component level.
 */

import { badgeTokens } from '@crazy-ui/theme';

export const badgeComponentTokens = {
  ...badgeTokens,
} as const;
